# Aegis self-host

Docker Compose stack for running Aegis on your own hardware.

Quick start:

```bash
cp .env.example .env
# edit .env — see ../docs/self-host.md for how to mint each value
docker compose up -d
```

Full walkthrough: [`docs/self-host.md`](../docs/self-host.md).
